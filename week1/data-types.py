#strings are data type that are non-numeric(not numbers).We always put them in 
#double quotes or sindle quotes to avoid errors.
strings='a','b','c','d','Fatawu','Tamale','Region'

#integers are data types that are numerical(numbers).we do not put them in quotes
integers=1,2,3,4,5,6,6,7,8,9,10

#floats are data types that are decimal in nature they are called floating points in python
float=1.0,2.6,5.7,3.5
